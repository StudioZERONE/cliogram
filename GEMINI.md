# KLIOGRAM 프로젝트 AI 에이전트 개발 & 아키텍처 헌법 (Project Conventions)

> 본 문서는 KLIOGRAM 프로젝트에서 AI 에이전트와 개발자가 코드 작성, 디자인 시스템 적용, DB 쿼리 관리 및 지식 문서화 시 예외 없이 100% 최우선 준수해야 하는 프로젝트 공식 개발 헌법입니다.

---

## 0. 커뮤니케이션 & 커스텀 어조 수칙 (Human-centric Communication)

- **친근하고 따뜻한 호칭/어조**: 기계적인 "요청하신 대로" 대신 항상 **"말씀하신 대로"**를 사용함.
- **솔직하고 담백한 경청**: 과장이나 옹호 없이 지적사항과 단점을 객관적이고 담백하게 전달함.
- **이모지 사용 전면 금지 (No Emojis)**: 불필요한 시각적 소음을 유발하는 이모지(Emoji) 사용을 전면 금지하고, 깔끔하고 정갈한 텍스트 기반 마크다운(Markdown) 표현만 사용함.

---

## 1. 지식 문서 관리 컨벤션 (Vault Document Conventions)

1. **파일 번호 부여**: 가치와 업무 순서를 기준으로 모든 마크다운 파일명 앞에 두 자리 번호 (`01.`, `02.`, ...)를 부여함.
2. **개발일지 관리**: 개발일지는 `51.Journal/개발일지/` 하위에 `YYYY년 WW주차 개발일지.md` 양식으로 독립 관리함.
3. **개별 기록 접두어**: 주차별 종합일지 외의 개별 기록 문서는 `YYYYMMDD ` 공백 접두어를 붙임.
4. **하단 관련노트 고정**: 관련 노트는 문서 중간이나 상단이 아닌 노트의 가장 마지막 라인에 위치시키며, 바로 위에 구분선(`---`)을 긋고 접두어를 **`관련노트:`**로 일관되게 표기함.
5. **대분류 구획 구분선 필수**: 문서 내 모든 대분류 제목(`## ` H2 수준 헤더) 바로 앞에는 구분선(`---`)을 긋어서 구역을 가독성 높게 구분함.

---

## 2. 서비스 정체성 & 브랜드 컬러 (Brand Identity)

- **서비스명 / 슬로건**: **KLIOGRAM** | *"고요히 흘러 마침내 숲이 될 하루"*
- **메인 브랜드 컬러**: **`#057a5d`** (Light Mode) / **`#10b981`** (Dark Mode)
- **개발/운영 비주얼 인디케이터**: 개발 DB(`kliogram-dev`) 구동 시 헤더 및 로그인 카드에 `Dev Sys` 뱃지 및 인디케이터 적용.

---

## 3. 핵심 레이아웃 & 테마 아키텍처 (Layout & Theme System)

- **플로팅 오버레이 사이드바**: `fixed top-0 left-0 h-screen z-40`으로 뷰포트에 완전 고정되며, 호버 시 사이드바만 상단 오버레이로 슬라이드되어 본문 가로폭이 밀리지 않음.
- **스마트 테마 토글**: 기본 `system` 모드 추종 및 12시간 TTL 수동 오버라이드 캐싱 후 자동 복귀.

---

## 4. 3대 SQL 쿼리 파이프라인 & RLS 보안 (Triple-SQL & RLS)

1. **`sql/a_schema.sql` (공개)**: DDL 스키마, RLS 보안 정책 및 `handle_new_user()` 자동 프로필 생성 트리거.
2. **`sql/b_data_system.sql` (공개)**: 공통코드 그룹, 상세 시드 및 환율 마스터.
3. **`sql/c_data_migration.sql` (비공개 Ignore 🔒)**: 난독화된 마이그레이션 샘플 데이터 (`my_user_id` 귀속).

---

## 5. AI 에이전트 자율 수행 및 협업 원칙 (Core AI Rules)

1. **기존 소스 및 주석 보존**: 기존 코드, 주석 및 JSDoc 100% 수호.
2. **실증적 오류 진단**: 터미널 로그 및 스택 트레이스 confirmation 후 수정.
3. **실증 검증 필수**: 수정 후 반드시 `npm run build` 통과 검증 완료 후 보고.
4. **Universal AI Protection 🔒**: 문서 내 사용자가 기입해둔 계정/비밀번호/키 정보 100% 보존.
5. **자율 커밋 & 푸시 원칙 (Auto Push)**: 사용자가 명시적으로 "푸시 보류/금지"를 요청하지 않는 한, 구현 및 `npm run build` 검증 완료 후 즉시 `git commit` & `git push origin main`까지 자동 완료함.
6. **출근/퇴근 생명주기 기반 로컬 개발 서버 자율 관리 원칙 (Work Lifecycle & Auto Dev Server)**:
   - **출근 (Work Start)**: "출근", "나 왔어", "일 시작하자", "좋은 하루~" 등 출근 인사를 받거나 개발 시작 상황 감지 시 `http://localhost:3000` 로컬 개발 서버를 백그라운드 데몬 프로세스로 자율 가동함. 코드 커밋 완료 시에도 개발 서버 상태를 항상 신선하게 자동 재가동/유지함.
   - **퇴근 (Work End)**: "퇴근하자", "오늘은 여기까지", "수고했다 이제 쉬어", "이제 간다~" 등 업무 종료 인사를 받으면 `http://localhost:3000` 로컬 개발 서버 프로세스를 자동 종료(`kill`)하고 AI도 함께 세션을 마침.
   - **야근/새벽 (Overtime Edge Case)**: 새벽이나 야근 시간대 세션 구동 시 시점이 모호한 경우 개발자에게 질문하여 출근/야근 여부를 확인하거나 기본적으로 연장 야근 상태로 간주하여 구동을 지속함.
7. **엄격한 DB 마스터 진실 원칙 및 우회/가짜 임시 데이터 금지 (Strict DB Truth & No Silent Fallbacks)**:
   - DB 조회 시 데이터가 없거나 쿼리 대상이 올바르지 않을 때, 임시용 하드코딩 덤미 데이터(Fallback Mock Data)를 반환하거나 조용하게 인자를 우회 변환(Alias Remapping)하여 감추는 포장 행위를 전면 금지함.
   - 코드 오류나 미설정 상태가 개발 중 즉시 실증 감지될 수 있도록, 항상 DB의 실제 상태(`[]` 빈 배열 등)만을 엄격하게 직접 반환하고 표출함.

---
